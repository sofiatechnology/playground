export default function SupportPage() {
  return (
    <article>
      <h1 className="text-2xl font-semibold">Support</h1>
      <p className="mt-4 text-muted-foreground">
        SOFIA EDITOR is a browser-based playground for writing and running code. If something breaks
        while executing a snippet, try resetting the editor or switching languages, then run the
        code again.
      </p>
      <p className="mt-3 text-muted-foreground">
        Snippets you share are stored in this browser so you can reopen them from the Snippets page.
      </p>
    </article>
  );
}
